# Cloudflare Pages: DO NOT publish the repo root (exposes server code/secrets,
# and Pages can't run node server.js or /api/* anyway).
# Correct Pages setting: Build output directory = src/dashboard.
# Then "/" serves src/dashboard/index.html automatically — no rewrite needed.
# This file is kept only for legacy root-publish setups:
/ /src/dashboard/index.html 200
