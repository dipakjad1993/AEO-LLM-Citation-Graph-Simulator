# Full install (heavy ML extras). Default should be requirements-lite.txt.
# Heavy extras are lazy-loaded and optional:
#   torch + transformers + sentence-transformers (embeddings, default MiniLM instead)
#   umap-learn + hdbscan (anomaly clustering, sklearn fallback)
#   spacy en_core_web_trf (400MB, default en_core_web_sm)
-r requirements-lite.txt
spacy>=3.7.0
sentence-transformers>=3.0.0
torch>=2.3.0
tokenizers>=0.19.0
transformers>=4.41.0
umap-learn>=0.5.6
hdbscan>=0.8.33
plotly>=5.22.0
fsspec>=2024.5.0
