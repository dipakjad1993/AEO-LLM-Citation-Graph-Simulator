# Kept for backward compat: older cached index.html requested
# /src/dashboard/sections.js. Current index.html uses ./sections.js,
# so this rewrite is a no-op for fresh deploys and can be removed later.
/src/dashboard/sections.js /sections.js 200
